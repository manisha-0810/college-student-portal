import Link from "next/link";
import PageHeader from "@/components/page-header";
import { attendance, marks, student } from "@/lib/mock-data";
import { Icon } from "@/components/icons";
const pct = (a: number, t: number) => Math.round((a / t) * 100);
export default function DashboardPage() { const average = Math.round(attendance.reduce((sum, x) => sum + pct(x.attended, x.total), 0) / attendance.length); return <><PageHeader title="Dashboard" description="A quick overview of your academic journey."/><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Stat label="Attendance" value={`${average}%`} note="Overall this semester" icon="calendar" color="bg-violet-50 text-violet-600 dark:bg-violet-950"/><Stat label="Current CGPA" value="8.6" note="Up by 0.2 this term" icon="chart" color="bg-sky-50 text-sky-600 dark:bg-sky-950"/><Stat label="Enrolled subjects" value="4" note="Semester 4" icon="book" color="bg-emerald-50 text-emerald-600 dark:bg-emerald-950"/><Stat label="Fee balance" value="₹42.5k" note="Due 15 Jul 2025" icon="card" color="bg-amber-50 text-amber-600 dark:bg-amber-950"/></section><section className="mt-6 grid gap-6 xl:grid-cols-3"><div className="card xl:col-span-2"><div className="mb-5 flex items-center justify-between"><div><h2 className="section-title">Attendance overview</h2><p className="muted">Keep above 75% in every subject.</p></div><Link href="/attendance" className="text-sm font-semibold text-violet-600">View details</Link></div><div className="space-y-5">{attendance.map((item) => { const value = pct(item.attended, item.total); return <div key={item.subject}><div className="mb-2 flex justify-between text-sm"><span className="font-medium text-slate-700 dark:text-slate-200">{item.subject}</span><span className="text-slate-500">{value}%</span></div><div className="h-2.5 overflow-hidden rounded-full bg-slate-100 dark:bg-slate-800"><div className={`h-full rounded-full ${item.color}`} style={{ width: `${value}%` }}/></div></div>; })}</div></div><div className="card"><h2 className="section-title">Upcoming</h2><div className="mt-5 space-y-5"><Event date="24" month="Jun" title="Operating Systems quiz" time="10:00 AM · LH-302"/><Event date="26" month="Jun" title="DBMS assignment due" time="Before 11:59 PM"/><Event date="30" month="Jun" title="Networks lab" time="2:00 PM · Lab 4"/></div></div></section><section className="mt-6 grid gap-6 lg:grid-cols-2"><div className="card"><div className="mb-4 flex items-center justify-between"><h2 className="section-title">Recent results</h2><Link href="/marks" className="text-sm font-semibold text-violet-600">All marks</Link></div>{marks.slice(0,3).map(m => <div key={m.subject} className="flex items-center justify-between border-t border-slate-100 py-3 dark:border-slate-800"><div><p className="text-sm font-medium">{m.subject}</p><p className="text-xs text-slate-500">Internal {m.internal}/30 · Final {m.final}/70</p></div><span className="rounded-lg bg-emerald-50 px-2.5 py-1 text-sm font-bold text-emerald-700 dark:bg-emerald-950 dark:text-emerald-300">{m.grade}</span></div>)}</div><div className="card bg-gradient-to-br from-violet-600 to-indigo-700 text-white"><p className="text-sm font-medium text-violet-200">Student profile</p><h2 className="mt-2 text-xl font-bold">{student.name}</h2><p className="mt-1 text-sm text-violet-100">{student.course}</p><div className="mt-8 flex items-end justify-between"><div><p className="text-xs text-violet-200">Student ID</p><p className="mt-1 font-semibold">{student.id}</p></div><Link href="/profile" className="rounded-lg bg-white/15 px-3 py-2 text-sm font-semibold hover:bg-white/25">View profile</Link></div></div></section></> }
function Stat({ label, value, note, icon, color }: { label:string; value:string; note:string; icon:string; color:string }) { return <div className="card"><div className="flex items-start justify-between"><p className="muted">{label}</p><span className={`rounded-xl p-2.5 ${color}`}><Icon name={icon} className="h-5 w-5"/></span></div><p className="mt-4 text-2xl font-bold text-slate-900 dark:text-white">{value}</p><p className="mt-1 text-xs text-slate-500">{note}</p></div> }
function Event({ date, month, title, time }: { date:string; month:string; title:string; time:string }) { return <div className="flex gap-3"><div className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-violet-50 text-center dark:bg-violet-950"><b className="text-sm leading-3 text-violet-700 dark:text-violet-300">{date}</b><span className="-mt-2 text-[9px] font-bold uppercase text-violet-500">{month}</span></div><div><p className="text-sm font-medium">{title}</p><p className="mt-0.5 text-xs text-slate-500">{time}</p></div></div> }
