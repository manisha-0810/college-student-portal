import { NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";
import { requireAdmin } from "@/lib/admin-auth";
export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { if (!await requireAdmin()) return NextResponse.json({ error: "Unauthorized" }, { status: 401 }); try { const { id } = await params; const { code, name, credits } = await request.json(); return NextResponse.json(await prisma.subject.update({ where: { id }, data: { ...(typeof code === "string" && code ? { code: code.trim().toUpperCase() } : {}), ...(typeof name === "string" && name ? { name: name.trim() } : {}), ...(credits ? { credits: Number(credits) } : {}) } })); } catch { return NextResponse.json({ error: "Unable to update subject." }, { status: 400 }); } }
export async function DELETE(_: Request, { params }: { params: Promise<{ id: string }> }) { if (!await requireAdmin()) return NextResponse.json({ error: "Unauthorized" }, { status: 401 }); try { const { id } = await params; await prisma.subject.delete({ where: { id } }); return new NextResponse(null, { status: 204 }); } catch { return NextResponse.json({ error: "Unable to delete subject with linked records." }, { status: 400 }); } }
